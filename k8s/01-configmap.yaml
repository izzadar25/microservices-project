apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: microservices-demo
data:
  APP_VERSION: "1.0.0"
  LOG_LEVEL: "info"
  BACKEND_HOST: "backend"
  BACKEND_PORT: "5000"
  BACKEND_URL: "http://backend:5000"
